













        #region                 Открываем XML Файл через LiquidTechnologies. Ну и хуйня за 600$ !!!

using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace XMLReaderCS
{
    class LiquidTech
    {


        /*
        private void OpenXML_WithLIB()
        {
            if (openFileDialog1.ShowDialog(this) == DialogResult.OK)
            {
                ClearControls();
                textBox1.Text = openFileDialog1.FileName;

                if (Path.GetExtension(openFileDialog1.FileName).Equals(".xml"))
                {
                    this.MifPolygons.Defintion = Path.GetFileName(openFileDialog1.FileName);
                    saveFileDialog1.FileName = Path.GetFileNameWithoutExtension(this.MifPolygons.Defintion) + "_mif";

                    try
                    {
                        // load the xml from a file into the object (the root element in the
                        // xml document must be <ElmBaseElement>.

                        //LiquidTechnologies.Runtime.Net40.XmlObjectBase elm =  KVZU_v06Lib.ClassFactory.FromXmlFile(openFileDialog1.FileName);

                        // create an instance of the class to load the XML file into
                        // tns.KVZU elm = new tns.KVZU();

                        // load the xml from a file into the object (the root element in the
                        // xml document must be <bookstore>.
                        // elm.FromXmlFile(openFileDialog1.FileName);

                    }
                    catch (InvalidOperationException ex)
                    {
                        //Не КПТ v09 ли это?
                        toolStripStatusLabel2.Text = ex.Message;
                    }
                }

            }
        }
        */

    }
}

#endregion